name: Fantasy v1.6.5 RED
on:
  push:
    branches:
      - hotfix-v1.6.5-startup-and-tap-architecture
jobs:
  red:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node --test tests/startup-tap-architecture.test.js
